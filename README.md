# File-System-Changer
        ENG 
Use this to change the file system of your partitions. Create GPT or MBR formats, clean partitions, format to FAT32 with no size limit, NTFS, eXfat, ext1, ext2, ext3, ext4 for Linux

        ES
Usa esto para cambiar el sistema de archivos de tus particiones. crea formato GPT o MBR, limpia particiones, formatea en FAT32 sin límite de tamaño, NTFS,eXfat ext1, ext2, ext3, ext4 para linux
        
        PT
Use esta ferramenta para alterar o sistema de arquivos das suas partições. Crie formatos GPT ou MBR, limpe partições, formate para FAT32 sem limite de tamanho, NTFS, eXfat, ext1, ext2, ext3 e ext4 para Linux.
