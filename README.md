# File-System-Changer
Use this to change the file system of your partitions. Create GPT or MBR formats, clean partitions, format to FAT32 with no size limit, NTFS, eXfat, ext1, ext2, ext3, ext4 for Linux
